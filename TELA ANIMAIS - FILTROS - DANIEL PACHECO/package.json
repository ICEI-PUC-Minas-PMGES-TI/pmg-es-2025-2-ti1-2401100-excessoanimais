{
  "name": "tela-animais-filtros",
  "version": "1.0.0",
  "description": "Sistema de adoção de animais com filtros",
  "main": "index.html",
  "scripts": {
    "server": "json-server --watch db.json --port 3001",
    "dev": "json-server --watch db.json --host 0.0.0.0 --port 3001",
    "start": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
